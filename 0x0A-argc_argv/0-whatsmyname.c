#include <stdio.h>

/**
 * main - check code
 * @argc: number of arguments in program
 * @argv: array with the arguments of program
 *
 * Return: always 0
 **/
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
